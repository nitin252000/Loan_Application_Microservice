server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/accountdb
    username: root
    password: oracle

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  application:
    name: LOAN-SERVICE